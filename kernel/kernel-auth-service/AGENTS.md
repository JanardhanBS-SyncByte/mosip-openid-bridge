# kernel-auth-service

`/v1/authmanager` · `:8091`. Parent: [`../AGENTS.md`](../AGENTS.md). Chart: [`../../helm/authmanager`](../../helm/authmanager/AGENTS.md). Main `AuthBootApplication`. IAM `KeycloakImpl`. Tomcat. ZIP layout. Adapter is a POM unpack (not Docker wget). Path change → Helm/deploy.
