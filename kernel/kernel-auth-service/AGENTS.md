# kernel-auth-service

`:8091` `/v1/authmanager`. `AuthBootApplication` · `KeycloakImpl`. Adapter: POM unpack (not wget). Path → Helm/deploy. Local: `run-local.sh` / `run-local.bat` (`init` `start` `smoke` `stop`; no cluster GC flags).
