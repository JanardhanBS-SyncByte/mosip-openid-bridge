# kernel-auth-service

`:8091` `/v1/authmanager`. `AuthBootApplication` · `KeycloakImpl`. Adapter: POM unpack (not wget). Path → Helm/deploy. Local: `run-local.sh` / `run-local.bat` (compile + tests + run; no cluster GC flags).
