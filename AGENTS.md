# mosip-openid-bridge

Archived pointer. Java, Helm (`authmanager`), and deploy scripts live in [mosip/commons](https://github.com/mosip/commons) (`kernel/`, `helm/authmanager`, `deploy/kernel`). See [README.md](README.md). Do not add sources here.
