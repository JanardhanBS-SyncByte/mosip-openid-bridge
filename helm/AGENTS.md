# helm/

Chart `authmanager`. Parent: [root](../AGENTS.md). Install: [`deploy/AGENTS.md`](../deploy/AGENTS.md).

Istio `/v1/authmanager`. Image `kernel-auth-service`. Align `CHART_VERSION` with `deploy/install.sh`.

**Do not:** commit secrets; change port/health without probes.
